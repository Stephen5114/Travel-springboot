<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AdminMapper">
    <insert id="insert">
        insert into admin (username, password, name, phone, email, role, avatar)
        values(#{username}, #{password}, #{name}, #{phone}, #{email}, #{role}, #{avatar})
    </insert>

    <update id="updateByid">
        UPDATE admin SET username = #{username}, password = #{password}, name = #{name}, phone = #{phone}, email = #{email}, avatar = #{avatar}
        WHERE id = #{id}
    </update>

    <select id="selectAll" resultType="com.example.entity.Admin">
        SELECT * FROM admin
        <where>
            <if test="username != null and username != ''">username like concat('%', #{username}, '%') </if>
            <if test="name != null and name != ''">and name like concat('%', #{name}, '%') </if>
            <if test="ids != null and ids != ''">
                and id in
                <foreach collection="idsArr" open="(" close=")" separator="," item="id">
                    #{id}
                </foreach>
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>